---
title: Counter Demo
date: "2026-05-27"
description: A simple React experiment embedded in MDX.
tags: [react, demo]
---

import { useState } from "react";

export function Counter() {
  const [count, setCount] = useState(0);
  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(c => c + 1)}>Increment</button>
    </div>
  );
}

Here is a live React component embedded in MDX:

<Counter />
